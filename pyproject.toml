[project]
name = "template-pipeline"
version = "0.1.0"
description = "Reusable PySpark pipeline template for execution on the data platform."
requires-python = ">=3.10,<3.11"
dependencies = []

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core==2.4.0"]
build-backend = "poetry.core.masonry.api"
